#include <stdio.h>
void sortArray(int arr[],int n)
{
  int i,j,temp;
  for(i=0;i<n-1;i++)
  {
    for(j=0;j<n-i-1;j++)
    {
      if (a[j]>a[j+1])
      {
        temp=a[j];
        a[j]=a[j+1];
        a[j+1]=temp;
      }
    }
  }
}
int main()
{  
  int a[100],n,i;
  printf("Enter the array size:\n");
  scanf("%d",&n);
  printf("Enter the array size:\n");
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  sortArray(arr,n);
 printf("Sorted array :\n");
  for(i=0;i<n;i++)
     printf("%d",a[i]);
    return 0;
}



